import React from 'react';
import { useLoaderData } from 'react-router-dom';

const OrderReview = () => {

    return (
        <div>
            
        </div>
    );
};

export default OrderReview;